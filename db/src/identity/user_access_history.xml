<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ========== TABLE CREATION ========== -->

    <changeSet id="user_access_history:table_creation" author="doc-assembly">
        <comment>Create user_access_history table - tracks recent resource access for quick navigation</comment>
        <createTable tableName="user_access_history" schemaName="identity">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="user_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="entity_type" type="VARCHAR(20)">
                <constraints nullable="false"/>
            </column>
            <column name="entity_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="accessed_at" type="TIMESTAMPTZ" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
        </createTable>
    </changeSet>

    <!-- ========== FOREIGN KEYS ========== -->

    <changeSet id="user_access_history:add_fk_constraint:user_id" author="doc-assembly">
        <comment>FK to users - cascade delete when user is removed</comment>
        <addForeignKeyConstraint
            baseTableSchemaName="identity"
            baseTableName="user_access_history"
            baseColumnNames="user_id"
            constraintName="fk_user_access_history_user_id"
            referencedTableSchemaName="identity"
            referencedTableName="users"
            referencedColumnNames="id"
            onDelete="CASCADE"/>
    </changeSet>

    <!-- ========== UNIQUE CONSTRAINTS ========== -->

    <changeSet id="user_access_history:add_unique_constraint:user_entity" author="doc-assembly">
        <comment>One entry per user/entity_type/entity_id - enables UPSERT semantics</comment>
        <addUniqueConstraint
            schemaName="identity"
            tableName="user_access_history"
            columnNames="user_id, entity_type, entity_id"
            constraintName="uq_user_access_history_user_entity"/>
    </changeSet>

    <!-- ========== CHECK CONSTRAINTS ========== -->

    <changeSet id="user_access_history:check_constraint:entity_type" author="doc-assembly">
        <comment>Ensure entity_type is valid (TENANT or WORKSPACE)</comment>
        <sql>
            ALTER TABLE identity.user_access_history
            ADD CONSTRAINT chk_user_access_history_entity_type
            CHECK (entity_type IN ('TENANT', 'WORKSPACE'));
        </sql>
        <rollback>
            ALTER TABLE identity.user_access_history
            DROP CONSTRAINT IF EXISTS chk_user_access_history_entity_type;
        </rollback>
    </changeSet>

    <!-- ========== INDEXES ========== -->

    <changeSet id="user_access_history:index_creation:user_entity_type_accessed" author="doc-assembly">
        <comment>Composite index for efficient recent access lookups by user and entity type</comment>
        <createIndex indexName="idx_user_access_history_user_entity_accessed" tableName="user_access_history" schemaName="identity">
            <column name="user_id"/>
            <column name="entity_type"/>
            <column name="accessed_at" descending="true"/>
        </createIndex>
    </changeSet>

</databaseChangeLog>
