<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!-- ========== TABLE CREATION ========== -->

    <changeSet id="folders:table_creation" author="doc-assembly">
        <comment>Create folders table - hierarchical file system for organizing resources</comment>
        <createTable tableName="folders" schemaName="organizer">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="workspace_id" type="uuid">
                <constraints nullable="false"/>
            </column>
            <column name="parent_id" type="uuid">
                <constraints nullable="true"/>
            </column>
            <column name="name" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>
            <column name="path" type="TEXT">
                <constraints nullable="true"/>
            </column>
            <column name="created_at" type="TIMESTAMPTZ" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="TIMESTAMPTZ"/>
        </createTable>
    </changeSet>

    <!-- ========== FOREIGN KEYS ========== -->

    <changeSet id="folders:add_fk_constraint:workspace_id" author="doc-assembly">
        <comment>FK to workspaces</comment>
        <addForeignKeyConstraint
            baseTableSchemaName="organizer"
            baseTableName="folders"
            baseColumnNames="workspace_id"
            constraintName="fk_folders_workspace_id"
            referencedTableSchemaName="tenancy"
            referencedTableName="workspaces"
            referencedColumnNames="id"
            onDelete="CASCADE"/>
    </changeSet>

    <changeSet id="folders:add_fk_constraint:parent_id" author="doc-assembly">
        <comment>Recursive FK for folder hierarchy</comment>
        <addForeignKeyConstraint
            baseTableSchemaName="organizer"
            baseTableName="folders"
            baseColumnNames="parent_id"
            constraintName="fk_folders_parent_id"
            referencedTableSchemaName="organizer"
            referencedTableName="folders"
            referencedColumnNames="id"
            onDelete="CASCADE"/>
    </changeSet>

    <!-- ========== INDEXES ========== -->

    <changeSet id="folders:index_creation:workspace_id" author="doc-assembly">
        <comment>Index for workspace-based lookups</comment>
        <createIndex indexName="idx_folders_workspace_id" tableName="folders" schemaName="organizer">
            <column name="workspace_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="folders:index_creation:parent_id" author="doc-assembly">
        <comment>Index for hierarchical navigation</comment>
        <createIndex indexName="idx_folders_parent_id" tableName="folders" schemaName="organizer">
            <column name="parent_id"/>
        </createIndex>
    </changeSet>

    <changeSet id="folders:add_unique_constraint:workspace_parent_name" author="doc-assembly">
        <comment>Unique folder names within same parent in workspace</comment>
        <sql>
            CREATE UNIQUE INDEX idx_folders_unique_name
            ON organizer.folders (workspace_id, COALESCE(parent_id, '00000000-0000-0000-0000-000000000000'::uuid), name);
        </sql>
        <rollback>DROP INDEX IF EXISTS organizer.idx_folders_unique_name;</rollback>
    </changeSet>

    <!-- ========== TRIGGER ========== -->

    <changeSet id="folders:trigger:updated_at" author="doc-assembly">
        <comment>Trigger to auto-update updated_at on modification</comment>
        <sql>
            CREATE TRIGGER trigger_folders_updated_at
            BEFORE UPDATE ON organizer.folders
            FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
        </sql>
        <rollback>DROP TRIGGER IF EXISTS trigger_folders_updated_at ON organizer.folders;</rollback>
    </changeSet>

    <!-- ========== PATH SUPPORT (Materialized Path) ========== -->

    <changeSet id="folders:index_creation:path" author="doc-assembly">
        <comment>B-tree index for path prefix searches (LIKE 'path/%')</comment>
        <createIndex indexName="idx_folders_path" tableName="folders" schemaName="organizer">
            <column name="path"/>
        </createIndex>
    </changeSet>

    <changeSet id="folders:function:compute_folder_path" author="doc-assembly">
        <comment>Function to compute and maintain folder paths</comment>
        <sql splitStatements="false">
            CREATE OR REPLACE FUNCTION organizer.compute_folder_path()
            RETURNS TRIGGER AS $$
            DECLARE
                parent_path TEXT;
                old_path TEXT;
                new_path TEXT;
            BEGIN
                -- Get parent's path (NULL if root folder)
                IF NEW.parent_id IS NOT NULL THEN
                    SELECT path INTO parent_path
                    FROM organizer.folders
                    WHERE id = NEW.parent_id;
                    NEW.path := parent_path || '/' || NEW.id::text;
                ELSE
                    NEW.path := NEW.id::text;
                END IF;

                -- On UPDATE: cascade path changes to descendants
                IF TG_OP = 'UPDATE' AND OLD.path IS DISTINCT FROM NEW.path THEN
                    old_path := OLD.path;
                    new_path := NEW.path;
                    UPDATE organizer.folders
                    SET path = new_path || substring(path FROM length(old_path) + 1)
                    WHERE path LIKE old_path || '/%';
                END IF;

                RETURN NEW;
            END;
            $$ LANGUAGE plpgsql;
        </sql>
        <rollback>DROP FUNCTION IF EXISTS organizer.compute_folder_path();</rollback>
    </changeSet>

    <changeSet id="folders:trigger:path" author="doc-assembly">
        <comment>Trigger to auto-maintain path on INSERT/UPDATE</comment>
        <sql>
            CREATE TRIGGER trigger_folders_path
            BEFORE INSERT OR UPDATE OF parent_id ON organizer.folders
            FOR EACH ROW EXECUTE FUNCTION organizer.compute_folder_path();
        </sql>
        <rollback>DROP TRIGGER IF EXISTS trigger_folders_path ON organizer.folders;</rollback>
    </changeSet>

    <changeSet id="folders:add_not_null:path" author="doc-assembly">
        <comment>Path is required (trigger fills it automatically)</comment>
        <addNotNullConstraint tableName="folders" schemaName="organizer" columnName="path"/>
        <rollback>
            <dropNotNullConstraint tableName="folders" schemaName="organizer" columnName="path"/>
        </rollback>
    </changeSet>

    <!-- ========== UTILITY FUNCTION ========== -->

    <changeSet id="folders:function:populate_folder_paths" author="doc-assembly">
        <comment>Utility function to populate paths for existing folders (for data repair)</comment>
        <sql splitStatements="false">
            CREATE OR REPLACE FUNCTION organizer.populate_folder_paths()
            RETURNS INTEGER AS $$
            DECLARE
                updated_count INTEGER;
            BEGIN
                WITH RECURSIVE folder_paths AS (
                    SELECT id, id::text as computed_path
                    FROM organizer.folders
                    WHERE parent_id IS NULL
                    UNION ALL
                    SELECT f.id, fp.computed_path || '/' || f.id::text
                    FROM organizer.folders f
                    JOIN folder_paths fp ON f.parent_id = fp.id
                )
                UPDATE organizer.folders f
                SET path = fp.computed_path
                FROM folder_paths fp
                WHERE f.id = fp.id AND (f.path IS NULL OR f.path != fp.computed_path);

                GET DIAGNOSTICS updated_count = ROW_COUNT;
                RETURN updated_count;
            END;
            $$ LANGUAGE plpgsql;

            COMMENT ON FUNCTION organizer.populate_folder_paths() IS
            'Populates or repairs folder paths using recursive CTE.
            Usage: SELECT organizer.populate_folder_paths();';
        </sql>
        <rollback>DROP FUNCTION IF EXISTS organizer.populate_folder_paths();</rollback>
    </changeSet>

</databaseChangeLog>
